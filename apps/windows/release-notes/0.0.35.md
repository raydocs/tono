# Tono 0.0.35

Joint test candidate with macOS 0.0.68.

- Controller `/delay` is advisory. Real TUN HTTPS owns Connected.
- Google / Cloudflare / Apple probes race; one success is enough.
- Shared protected-connectivity decision table in `tono-core`.
- Structured failure codes when the real TUN path is exhausted.
- Optional DIRECT policy after Connected.
- Update handoff journal for 0.0.34 → 0.0.35 service replacement.
- App crate renamed to `tono-windows`. Clash Verge leftovers remain only
  for leftover process sweep and historical dual-read.

Before connecting on a test machine, keep Restore Network /
`tono-service.exe --emergency-disarm` ready. Kill Switch stays fail-closed.
