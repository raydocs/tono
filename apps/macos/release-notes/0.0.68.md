# Tono 0.0.68 (build 68)

Joint test candidate with Windows 0.0.35.

- Controller `/delay` is advisory. Real multi-origin TUN traffic owns Connected.
- Google / Cloudflare / Apple probes race; one success is enough.
- Optional DIRECT policy no longer blocks first Connected.
- Session-dynamic mixed and controller ports.
- Node switch rolls back without restarting the core.
- Update handoff journal + Sparkle install gate.
- Helper 3.13.0 (System Configuration DNS, networksetup fallback). First
  launch of this build asks for an administrator password once.
- Redacted connection telemetry events in the periodic window.
