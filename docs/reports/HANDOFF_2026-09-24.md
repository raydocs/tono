# 交接：2026-09-24 晚轮

下一轮从这里开始。总账见 [FINDINGS_LEDGER §11](../FINDINGS_LEDGER.md)，每个 PR 的审查状态写在该 PR 的评论里。
审查原文与核实原文在维护者机器的 `~/Downloads/GitHub/tono-review-records/2026-09-24-late/`（`scratch/`、`hunt/`）。

## 1. 生产现状（只读 D1，2026-09-24）

- `exit_credential_rollout.phase = dual`。
- `exit_nodes` 只有 1 个 active 节点 `los-angeles-westwood`，`last_roster_at` 停在 2026-09-18。
  原因：Xray 26.3.27 的 `rmu` 不接受 `--email=`，exit-agent 每轮拒绝，吊销不执行、计量停报。
- 另有一台已上线、服务用户、但**未登记**在 `exit_nodes` 的节点（静态配置 46 个客户端，无 exit-agent，无计量，吊销不生效）。
  同机还有一个非 Tono 的 `family` 入站（8443），exit-agent 不碰它。
- 6 个账户有被吊销设备（#323 的 0077 会退役其共享凭据）；26 台在用设备，3 台凭据晚于该节点最后一次 ACK。
- 没有以 ` · hy2` 结尾的 home exit 行。
- Xray 最新正式版仍是 v26.3.27（之后均为 pre-release），节点已是该版本；不需要升级 Xray。

## 2. 必须按此顺序

1. **#563 fleet 列车**：TF-opus-2..8 已由 Codex 核实（2/3/6 P3，4/5/8 P2 均为 main 既有或少见运维顺序 → follow-up issue；
   7 为测试缺口 PLAUSIBLE，建议在列车补三个窄测试；2 为两列车 `revokeExitToken` 合并冲突，后合入者必须手工合并五参数版本）。Codex 对 rmu 修复（5bcc6b9d/924cafb3/b3299814）的复核结果在
   `scratch/verify-fleet-rmu-codex.md`；TF-opus-2..8 的核实在 `scratch/verify-train-fleet-opus-codex.md`。
   rmu 修复复核为 PARTIAL（回显可伪造成功），已由 42653897 改为整行匹配并同样修 `adu`（pytest 91）；需对 42653897 再做异厂商复核、CI 绿后合并（`--merge`）。
2. 把 `services/exit-agent/reconcile_and_report.py` 部署到 `los-angeles-westwood`（`/opt/tono-exit-agent/`，
   `tono-exit-agent.timer` 每分钟），确认 D1 `exit_nodes.last_roster_at` 前进、metering-ack 恢复。
3. **登记第二个节点（在当前 dual Worker 上、部署 #570 之前）**：Codex 核实 TC-anthropic-1 为 P1、TC-anthropic-3 为 P2
   （`scratch/verify-train-cp-opus-codex.md`）。在旧 Worker 上 POST exit-nodes，按原目录名和稳定 source ID 部署节点专属 token/agent，
   首次运行前备份配置并比对 roster 与现有 46 个客户端；完成对账、ACK 严格晚于最新设备凭据、真实设备认证通过。
   注意 agent 不会删除任意手工标签，dual 下可能保留 `shared-legacy`，不能仅凭 ACK 宣称静态旧授权已清理。
4. **#570 控制面列车**：修 TC-anthropic-1——readiness 门只看**本次响应所用目录快照**中的节点（hy2 归并到基名），
   目录内节点即使 ack=0 仍阻塞，退役账户不回落共享凭据；不要用 `last_roster_at > 0` 简单跳过。TC-anthropic-2 补断言
   （未就绪 503，完整 ACK 后 200 且用设备 UUID）。8fc72696 的两项为 PARTIAL（onboarding 并发窗口；存量行编辑），需补。
   #375（在 #563）与 #451 的 `revokeExitToken` 由后合入的列车手工合并（五参数版本）。合并后用 `deploy-control-plane-main.sh`
   部署（先迁移）；随后两节点再次拉取并应用新 Worker 的 roster；最后**无条件**用带 `expectedRevision` 的受审计 catalog PUT
   推进一次目录 revision 并刷新客户端（TC-anthropic-3：0078 改过滤集合不 bump，Windows 会拒收同 revision 不同 digest）。
   若必须先部署 #570，则必须先合上述 membership 修复，且第二节点先作为未发布节点预配。
5. **#567 macOS 列车**：追加一个提交修 TM-OpenAI-1（`.unverifiable` 跳过 PF 健康检查）、TM-claude-1、TM-claude-5，
   CI 绿后合并。TM-claude-2/4/6 开 follow-up issue；TM-claude-3 记为临时产品决定。
6. **#572 Windows 列车**：Opus 核实（`scratch/verify-train-win-opus.md`）结论：三项都不是列车合并引入。
   TW-OpenAI-2 = TW-G-1 → P2（#509 单独引入；修法：仅 `Consumed` 时写 `Uncertain`，`Replaced` 保持）；
   TW-OpenAI-1 → P3/PLAUSIBLE（#343 单独，隐蔽通道；修法：DHCPv4 许可限定 Dhcp 服务 SID）；
   TW-G-2 → P2，main 已有（三个安装器退出点补 `ReleaseManualLease`）。均可作 follow-up，不阻塞合并。
   Opus 自审另有 **TW-anthropic-1（P1，产品决定）**：#343 入向默认拒绝持久且开机重装，仅靠 RDP 管理的电脑连接后无法远程恢复；
   掉线的 RDP 会话仍算 owner 在线，控制台用户 1014 且无法释放。需维护者定：放行局域网 RDP，或在远程会话中拒绝/警告 Connect。
   合并前需 Codex 核实（`scratch/review-train-win-opus.md`）。

## 3. 待核实 / 待复审（结果文件在 `scratch/`）

| 对象 | 状态 | 结果文件 |
|---|---|---|
| H20-O（15）、H22-O（8）由 Codex 核实 | 运行中 | `hunt/verify-H20-H22-opus-by-codex.out.md` |
| #535/#539 返工 + 535-O-F1/F5、537-O-F3..F5、538-O-F1、N1 | 完成：N1（#516）P2 确认；535 返工部分修复，新 535R-C-F1/F2（P2，待 Anthropic 核实）；结论已评论到 #516/#535 | `scratch/verify-mac4-O2-codex.md` |
| #537/#538 返工 | 完成：537-O-F2 FIXED，537-O-F1 PARTIAL，新 537R-C-F1（P2，reassert 与激活查询竞态） | `scratch/verify-537-rework-codex.md` |
| WIN-A 六个 PR（#557 #565 #569 #571 #573 #577） | Codex（WA-OpenAI-1..4，已评论到 PR）与 Opus（WA-opus-1..3，均 P3）均完成；两边发现互相尚未核实。WA-OpenAI-1（#569 中间 junction 越界删除）Opus 未覆盖，需先核实 | `scratch/review-winA-codex.md`、`review-winA-opus.md` |
| MAC-A 四个 PR（#550 #562 #566 #579） | Codex + Grok 完成；#566 两家独立报出同一 P1（只扫 /Applications 顶层即判定已卸载），阻塞；#579 P1/P2 | `scratch/review-macA-codex.md`、`review-macA-grok.md` |
| 候选诊断 #578 #580 #581 | 完成：#581 两家独立报出内部版无退出开关（P1/P2，阻塞）；#580 关日志不撤销排队报告（P2，既有竞态） | `scratch/review-diag-*.md` |
| #513 #515 #516 #518 #520、Worker #523 #525 #528 #531 #534、jev-route PR #1 | **未产出结果**（会话结束时被终止），需重跑 Codex 复核 | — |

规则不变：发现由不同厂商核实；不确定即不确认；每个行为一个窄回归测试。

## 4. 新开 issue（已异厂商核实，未修）

#582–#596（H20/H21/H22）。连接相关优先：#582、#583、#584（控制面不可达/被污染时无法恢复）、#586（高）。

## 5. 仍需维护者决定

- 临时产品决定（严格选项）：#343 开保护断开 RDP；#354 第二用户无网；#460 会话被拒即挂起；#566 helper 仅在
  `/Applications` 无 Tono 时自释放（Codex 指出移动应用会误判，需改）；#579 不跨账户改绑；#569 删除所有本地账户数据；
  #557 不动无法证明属于 Tono 的代理；#573 仅确认后清孤立拦截；#578/#580/#581 内部版只默认开分类失败记录；
  TM-claude-3 公司 VPN 分流 DNS 冲突时暂停并保持 PF。
- 客户发布仍需 SHIP_PLAN G1–G3 的实机证据。
