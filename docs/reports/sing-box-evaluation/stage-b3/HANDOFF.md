# B3 只交付实验；原生与国内证据仍缺失

先读 [RESULTS.md](RESULTS.md) 的两批原值和 [CPU.md](CPU.md) 的热点证据。
复现入口及失败语义见 [工具说明](../../../../tooling/experiments/sing-box-bench/stage_b3/README.md)。
A/B/B2 未覆盖，不把旧短流和本次长流混成一个统计总体。

1. **本地主线程继续拥有两项 P2 的正式修复。** 本批没有产品补丁，也没有重新声明修复完成；
   已有复核保留在 [B AUDIT](../stage-b/AUDIT.md)。Linux 的独立 harness 不是原生产品回归。
2. Mac/Windows 需另测真实点击、IPC、特权保护、DNS、数据面验证到 Connected 的完整链路，
   以及取消/切换/崩溃/恢复时 PF/WFP 精确端点和 fail-closed。不以本批成功率关闭 G1/G2/G3。
3. 国内 TLS/UDP 可用性需要获授权国内测试端与专用身份；不能把同宿主无损 veth 当作运营商链路。
   本 Orb 没有 netem，因此未做丢包/抖动/限速注入，也不推断 Hy2 的国内收益。
4. CPU 热点只支持其直接可观察的归因。若需验证某个调度/唤醒机制的因果贡献，另建固定基线上的
   诊断批次，明确唯一修改并保留正常二进制。不能用当前样本直接替换产品依赖。
5. 没有启用 mutex/block 采样，不能称为“无锁竞争”；没有原生电量测量，不能称为“更省电”。
   只有两批 30s synthetic SSE，不能称为长期稳定性或真实 provider 的普遍胜者。

不开始内核迁移，不添加 1.2s fallback，不自动重放 POST，不推主分支、不建/合并 PR、不发布。
本次只上传独立实验分支，供主线程审阅后决定是否需要新的受控实验。
