## 2026-09-26 · 桌面客户版改为 0.0.74 / macOS build 74，发布构建必须带序列号

- 归属：G4（G4.1 冻结前置，G1–G3 一轮测试包的 PR-5）；macOS、Windows 发布 workflow 与源码版本。
- 来源：`main` `184b1a0c` → 分支 `chore/desktop-0.0.74-20260926`；红测试 `20fce7b5`，实现 `62a7a5bf`；未合 main。
- 缺陷修复：`macos-release.yml` / `windows-release.yml` 此前接受空的 `update_release_sequence`，产物没有 v1 安装下限，此后所有 v1 更新都会拒绝它；现在两者在 ref 门就拒绝。`macos-release.yml` 去掉 tag-push 触发：tag 构建拿不到序列输入，而发布时创建 `tono-macos-<v>-build<n>` 不应再触发构建。
- 新增/优化：产品版本 0.0.74（macOS `CFBundleVersion` 74；老板定为 74，因 `tono-macos-0.0.73-build73` 已指向 `bdc75a4e`，不移动、不复用，`v0.0.73` 草稿同样不复用）。客户版发布说明 `build74.md` / `0.0.74.md`（中英）：B0 日志默认口径、「本版备用通道仅手动」、已知限制 #331（macOS）与 #352（Windows）、睡眠唤醒未认证、SmartScreen、系统要求、Helper 授权。RELEASE_LINES 的 G4 步骤改为发布时才打标签、发已验收产物。
- 工程与测试：candidate 常量、Windows candidate / smoke 产物名、`test-windows-candidate-install.ps1` 与 `macos-candidate-workflow.test.rb` 夹具随版本改到 0.0.74；新增一处回归（同一 Ruby 测试执行两个 release workflow 的 ref 门：空序列拒绝、`7401` 通过）。
- 验证：MacBook 本地 `ruby tooling/scripts/tests/macos-candidate-workflow.test.rb` 在 `20fce7b5`（旧 workflow）exit 1「macos-release.yml admitted a release build without update_release_sequence」；只换新 macOS workflow 时 exit 1「windows-release.yml admitted …」；在 `62a7a5bf` 全部通过。`python3 tooling/scripts/verify-desktop-version.py --expected 0.0.74` PASS。`windows-ci-paths.test.cjs`、原生构建未在本机执行，见 PR 的 CI。
- 候选/发布：无新包，仅源码。
- 剩余限制：DECISIONS 里 #331 条目仍写「0.0.73 带已知限制发布、#331 在 0.0.74 继续」；0.0.74 改号、#352 不挡发布的 owner 条目未在本 PR 写入。SHIP_PLAN §6 勾选行仍写 0.0.73（agent 不改）。
