| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| 535R-C-F3-R1 | 最后一次拒绝计数检查之后，运行时启动还要等几次；期间来的拒绝会被过期启动的 `.ready` 或 `fail` 的 `.error` 覆盖 | in-PR | [#601](https://github.com/raydocs/tono/issues/601)，[#653](https://github.com/raydocs/tono/pull/653) | 低·推导 | 只覆盖 cloud-only 启动与 `activateCloudFallback`；未实机 |

#601 评论项「535R-C-F3 residual」：Codex 核实 f587b441 时发现。修复：`startCloudOnlyRuntimeThrowing` 每个 await 之后与失败路径上复查 `entitlementRefusals`，有新拒绝即按取消处理。
