| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| H22-O-F2 | Windows 未装 VC++ x64 运行库的机器无法安装：`tono-service-install.exe`、`tono-service.exe`、`tono-service-uninstall.exe` 直接导入 `VCRUNTIME140.dll`（非延迟加载），而 NSIS 在 `.onInit` 运行门禁，早于 `CheckAndInstallVSRuntime` 小节，门禁无法启动，安装以 76 中止 | in-PR | [#658](https://github.com/raydocs/tono/pull/658) | 中·已确认(astra6 分诊 + Opus 复核，fb5e8485 候选导入表) | 修复：`apps/windows/service/.cargo/config.toml` 为 `x86_64-pc-windows-msvc` 加 `+crt-static`；两个本地构建脚本改为在 service 目录运行 cargo；`release:preflight --payload-only` 检查安装包内每一份 tono-service*.exe 不导入 `VCRUNTIME*`/`MSVCP*`。保留 VC++ 运行库安装小节（Tono.exe/WebView 未证明不需要）。未在无运行库的干净 Windows 上实机安装 |

来源：H22 Opus 席位；2026-09-26 astra6 全量分诊 + Opus 交叉核实确认为 major、0.0.73 发布阻断（见 changelog.d/2026-09-26-release-decisions.md），并解析 fb5e8485 候选安装包的 PE 导入表确认。等级按账本规则记「中」（安装受阻，fail-closed，无泄漏）。回归检查是候选/发布工作流里的 payload 预检。
