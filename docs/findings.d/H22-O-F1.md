| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| H22-O-F1 | Windows BFE 停止或禁用时安装失败：NSIS `.onInit` 的 `--manual-update-gate` 先读 WFP（RPC 到 BFE），`TONO_BFE_NOT_RUNNING` 使门禁以通用「无法安装」退出 76；原有的 BFE 自修复（df1b8976）在 b6b42ea0 之后排在门禁读 WFP 之后，永远跑不到；App 横幅「修复」走同一路径同样失败 | in-PR | [#658](https://github.com/raydocs/tono/pull/658) | 中·已确认(astra6 分诊 + Opus 复核) | 修复：门禁和无参/`--replace-runtime` 修复在第一次读 WFP 前先把 BFE 拉起（停止则启动，StartPending 等待，30 秒上限）。BFE 被禁用时不改启动类型（暂定决定，见 DECISIONS.md），门禁仍拒绝（读不到 WFP 不当作「无残留过滤器」），退出 79，NSIS 显示 BFE 专用提示和 `sc.exe config BFE start= auto`、`sc.exe start BFE`；卸载门禁拿到 79 时仍显示通用卸载拒绝文案。未实机验证 BFE 停止/禁用的安装 |

来源：H22 Opus 席位；2026-09-26 astra6 全量分诊 + Opus 交叉核实确认为 major、0.0.73 发布阻断（见 changelog.d/2026-09-26-release-decisions.md）。等级按账本规则记「中」（安装受阻，fail-closed，无泄漏）。回归测试 `update_executor::tests::update_manual_gate_brings_bfe_up_before_reading_wfp`。
