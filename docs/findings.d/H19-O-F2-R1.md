| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| H19-O-F2-R1 | Windows 确认清除无主拦截后，`--retire-orphaned-owner` 写停止状态失败，旧 owner 仍 `core_should_be_running=true`；重跑时已无过滤器，门控回 77（去断开），但已没有 Service/App 可断开 | in-PR | [#602](https://github.com/raydocs/tono/issues/602)，[#637](https://github.com/raydocs/tono/pull/637) | 低·已确认 | 此态下 78 确认框文案仍写「拦截还在」；静默安装仍拒绝；未实机 |

#573（H19-O-F2）的残留，#602 评论项「#573 residual (P2)」：Codex 核实 aeb3445e 时发现，作为少见路径接受为后续。
修复：`begin_manual` 的 owner 检查拆成纯函数 `runnable_owner_refusal`，没有 Tono Service 时回 78（`OrphanedProtection`），
重走已有的确认恢复；有 Service 或 SCM 读不出时仍回 77。pending/租约检查仍在此之前，过滤器检查须先通过，均未改。
