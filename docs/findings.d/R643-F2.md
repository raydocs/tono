| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| R643-F2 | macOS helper 被替换 token 的签发区间用 pfctl 自己的 `time()` 减年龄计算；墙钟在查询期间回拨后又拨回时区间整体平移，`sameIssue` 失败走遗忘分支，未回答的 `-X` 不再重试；时钟超前时区间向过去扩展，同 PID 同进程名的再签发可能被重叠判定为同一次签发 | open | [#601](https://github.com/raydocs/tono/issues/601)，[#643](https://github.com/raydocs/tono/pull/643) | 低·已确认 | 前者只漏引用（保守方向）；后者要求 PID 与进程名同时相同的再签发恰落在秒级偏移内；只有颠倒窗口被拒绝；未实机 |

来源：#643 续审 run `11ca9581`（grok:F4，Opus 复核确认）。同轮另有记录/未记录 token 重试仍只按数值匹配、`gaveUp` 取样时机、自测覆盖等建议级项，已写在 changelog 片段的剩余限制。按停止规则（#654）记为 open 后合并。
