| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| R643-F1 | macOS helper arm 的 hold 路径仍用 `pfEnableReferenceListed` 判断未记录 token 是否还在：pfctl 非 0 退出或以退出 0 只打印 `DIOCGETSTARTERS` 警告时返回 false，`unrecordedPFEnableReference` 被清掉，该 token 此后无路径释放 | open | [#601](https://github.com/raydocs/tono/issues/601)，[#643](https://github.com/raydocs/tono/pull/643) | 低·已确认 | 后果是 PF 启用引用残留（disarm 后 PF 仍启用、不含 Tono 规则），不放宽保护、不漏流量；修法是 hold 路径改用只认完整列表的 `pfEnableReferenceListing`；未实机 |

来源：#643 续审 run `11ca9581`（opus:F1 = grok:F2，Codex/Grok 复核确认）。按 AGENTS.md 停止规则（#654）在修复轮之后记为 open 后合并。
