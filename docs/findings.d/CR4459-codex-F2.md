| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| CR4459-codex-F2 | #635 收紧归属后，登录提交时本机标记写入失败只记警告，合法本机会话无标记，下次启动被当成外来会话：登出并释放防护 | in-PR | [#409](https://github.com/raydocs/tono/issues/409)，[#642](https://github.com/raydocs/tono/pull/642) | 低·推导 | 登录在释放防护和丢弃上一账户目录之前一律写入待定标记 `pending:<代次>`（已有已提交标记也改写，并记下原内容），写不进则拒绝（防护与上一账户不动，需重新获取验证码）；待定标记不作证，旧格式标记仍作证；会话未进入凭据库的失败（释放被拒、被取代、`client.adopt` 失败）写回任何登录在途之前的标记（重叠登录由后来者继承，比较并交换）；adopt 后登录照常完成，每个接纳的登录有唯一的提交任务，5 秒起倍增、封顶 300 秒一直重试到提交或更新的登录被接纳（与周期同步、账户状态、登录代次无关），落库后才改为已提交；始终未落库或提交前进程退出时，下次启动走 main 既有的无归属会话路径（NoToken：登出，Service 报告 Armed 时释放已存防护），需重新登录；提交任务的重试与在途期间的提交无单元测试；未实机 |

来源：合并回归审查（区间 `f2e24512...fb5e8485`，jev-route run `4459fadd`）的 codex:F2，Opus 复核由 major 降为 minor
（触发前提是登录那一刻 `%LOCALAPPDATA%` 下的标记目录不可写，而凭据库与漫游目录写入都成功）。
#642 评审 d9ba823a（opus:F1、codex:F1/F2/F3）后续修：拒绝点前移到不可逆步骤之前、失败时撤销本次新写的标记、前端映射错误并清空已消耗的验证码，见 changelog 条目续记。
#642 续审 ad7be8fc（opus:F1、codex:F1，同一根因：标记只表示文件存在，未绑定本次登录已存下的会话）后续修：
`TonoInner.sign_in_marker_pending` 记录写了标记、尚未存下会话的登录代次，被取代的登录只要标记仍归自己就撤销，
后来的登录接管待定标记而不判成 `Existing`，见 changelog 条目第二条续记。
#642 续审 147a047f（codex:F1：待定状态只在内存，崩溃或撤销失败后重启，标记仍为旧账户作证）后续修：标记文件本身写待定/已提交，
启动时待定标记即 `NotOwned`，见 changelog 条目第三条续记。
#642 评审 c0e92466（opus:F1/F2、codex:F1/F2：提交早于落库；提交失败被吞仍返回成功）后续修：提交等待写库落盘，任何 adopt 之后的失败返回
`TONO_SIGN_IN_NOT_SAVED` 并保持标记待定，见 changelog 条目第四条续记。
#642 评审 44d47166（opus:F1、codex:F2 major：adopt 后返回 NOT_SAVED 留下半登录状态；opus:F2、codex:F1 minor）后续修：撤回 adopt 后的失败返回，
标记一律先写待定、后台落盘后提交，见 changelog 条目第五条续记。
#642 评审 c28a72f7（codex:F2、opus:F1、opus:F3/codex:F1，均 minor）后续修：读错误照常重试、adopt 之前失败写回原标记、周期同步继续尝试提交，
见 changelog 条目第六条续记。
#642 评审 e812b461（opus:F1/F2、codex:F1/F2，均 minor：提交路径随文件内容、周期同步或登录代次结束；撤销写回紧邻内容）后续修：
进程内标记归属集中到 `SessionMarker`（在途登录与待提交登录），提交任务只在已提交或更新的登录被接纳时结束，撤销写回任何登录在途之前的内容，见 changelog 条目第七条续记。
