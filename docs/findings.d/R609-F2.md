| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| R609-F2 | macOS helper 的 `pfctl` 执行器等待无期限；`/core/stop` 先撤回许可，卡住的 `pfctl` 挂住 Core 停止和 helper 唯一请求线程 | in-PR | [#601](https://github.com/raydocs/tono/issues/601)，[#639](https://github.com/raydocs/tono/pull/639) | 中·推导 | Codex、Grok 复核 #609 时发现；改为 15 s 期限（只读查询 3 s），超时 SIGTERM→SIGKILL 并按失败处理；复核后，没有回答的查询不再读成「否」，不据此删意图、释放或遗忘引用、`pfctl -d`，`-E` 超时按 PID 找回 token；写操作卡住时 `/core/stop` 仍可超过 App 的 6 s；`-E` 找回依赖 `pfctl -s References` 行格式，未实机核对；回归不覆盖 SIGKILL 升级；未实机 |
