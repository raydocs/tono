| ID | 问题（一句） | 状态 | Issue / PR | 等级 | 剩余限制 |
|---|---|---|---|---|---|
| R639-O1 | macOS helper 的 `-E` 超时找回只按 PID + 进程名认领 `pfctl -s References` 的行，同次开机 PID 复用时会认领并在 disarm 时 `-X` 别的程序的 token；PID 被无关长寿进程复用时待定获取永不结算（= R639-F1） | in-PR | [#601](https://github.com/raydocs/tono/issues/601)，PR 待开 | 低·推导 | 改为子进程退出（终止回调）后才结算，只认领按 TIMESTAMP 年龄推算的签发秒整段落在子进程存活期内的唯一一行，证明不了就不认领（泄漏一个 token，不误释放）；TIMESTAMP 为年龄来自 pfctl 格式串与公开样例，未在实机 root 下核对；墙钟回拨叠加 PID 复用未防；未实机 |

#639 复核：Opus opus:F1 发现，Codex 复核确认；Codex codex:F1 同一问题（Opus 复核降为 minor）。合并回归审查 run `4459fadd` 复报（opus:F2，Codex 复核确认）。
